# Feature: tray-ui (v2)

## Goal

Provide a Windows tray and macOS menu-bar UI to view per-account status and switch accounts without using the CLI directly.

## Work Items

This feature is planned for wave `w2-tray-ui`. Work items will be added once the v1 CLI surface area is stable.
